export type Period = { from: string; to: string; timeZone: string }
export type Metrics = { revenue: string; cost: string; grossProfit: string; salesCount: number; averageCheck: string | null; margin: number | null }
export type Manager = { id: number; name: string; team: string; position: string; isActive: boolean; initials: string; avatarUrl: string | null }
export type Dashboard = { period: Period; currency: string; rankingBy: string; kpis: Metrics & { bestManager: Manager | null }; ranking: { rank: number | null; manager: Manager; metrics: Metrics }[]; series: { date: string; revenue: string; grossProfit: string; salesCount: number }[]; categories: { id: number; name: string; revenue: string; grossProfit: string; salesCount: number; quantity: number }[]; topProducts: { id: number; name: string; categoryId: number; revenue: string; grossProfit: string; salesCount: number; quantity: number }[]; comparison: { metrics: Metrics; change: { revenuePercent: number | null; grossProfitPercent: number | null; salesCountPercent: number | null; averageCheckPercent: number | null; marginPoints: number | null } } }
export type Sale = { id: number; soldAt: string; manager: Manager; customer: { name: string; company: string; segment: string }; status: string; includedInKpis: boolean; amount: string; grossProfit: string; items: { productName: string; quantity: number; unitPrice: string; unitCost: string }[] }
export type SalesResponse = { items: Sale[] }
